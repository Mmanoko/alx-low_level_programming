#include "main.h"

/**
 * swap_int - swaps values of two integers
 * @a: The first integer
 * @b: second integer
 *
 * Return: nothing
 */

void swap_int(int *a, int *b)
{
	int tem = *a;

	*a = *b;
	*b = temp;
}
